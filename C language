#include<stdio.h>
main()
{
int num, even_sum=0 odd_sum=0;
printf("number:");
scanf("%d",&num);
for(int i=1;i<=n; i++)
{
if(i%2==o)
even_sum+=i;
else 
odd_sum+=i;
}
printf("sum of even number:%d\n",even_sum);
printf("sum of odd number:%d\n",odd_sum);

return 0;
}
